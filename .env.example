# ── Django ────────────────────────────────────────────────────────────────────
DEBUG=False
DJANGO_SECRET_KEY=GERE_UMA_CHAVE_AQUI_python_-c_"from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
ALLOWED_HOSTS=seudominio.com.br,www.seudominio.com.br
SITE_URL=https://seudominio.com.br

# ── Google Maps / Places API ──────────────────────────────────────────────────
GOOGLE_API_KEY=AIzaSy...

# ── Banco de dados ────────────────────────────────────────────────────────────
# Deixe vazio para usar o padrão (BASE_DIR/db.sqlite3)
# No Docker o docker-compose já injeta DB_PATH=/data/db.sqlite3 automaticamente
# DB_PATH=/data/db.sqlite3

# ── Evolution API (WhatsApp) ──────────────────────────────────────────────────
# URL interna do container (docker-compose) ou IP da máquina
EVOLUTION_API_URL=http://evolution:8080
EVOLUTION_API_KEY=CHAVE_GLOBAL_DA_EVOLUTION_API

# ── Stripe — Chaves de produção ───────────────────────────────────────────────
STRIPE_PUBLIC_KEY=pk_live_...
STRIPE_SECRET_KEY=sk_live_...
# Obtenha em: Dashboard Stripe → Developers → Webhooks → seu endpoint → Signing secret
STRIPE_WEBHOOK_SECRET=whsec_...

# ── Stripe — IDs de preço (Dashboard → Products) ─────────────────────────────
STRIPE_PRICE_ID=price_...             # Plano mensal Pro

# Créditos avulsos — preços normais
STRIPE_CREDITS_20_PRICE_ID=price_...
STRIPE_CREDITS_50_PRICE_ID=price_...
STRIPE_CREDITS_100_PRICE_ID=price_...

# Créditos avulsos — preços Pro (com desconto)
STRIPE_CREDITS_20_PRO_PRICE_ID=price_...
STRIPE_CREDITS_50_PRO_PRICE_ID=price_...
STRIPE_CREDITS_100_PRO_PRICE_ID=price_...
